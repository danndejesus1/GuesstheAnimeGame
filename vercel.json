{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd jemanime && npm install",
  "buildCommand": "cd jemanime && npm run build",
  "outputDirectory": "jemanime/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
